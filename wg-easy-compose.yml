version: "3.8"

services:
  wg-easy:
    image: ghcr.io/wg-easy/wg-easy
    container_name: wg-easy
    environment:
      - INIT_ENABLED=true
      - INIT_USERNAME=stephen
      - PASSWORD_HASH=$$2a$$12$$QbC4Ye2G0ED1d.IIOzF7NOvxCTkSBPmBsJOIR8gF8YeE/M31b8Ya2
      - WG_HOST=sjtaylor1471.synology.me
    volumes:
      - /volume1/docker/wg-easy:/etc/wireguard
    ports:
      - 51820:51820/udp
      - 51821:51821/tcp
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
    restart: unless-stopped
